# Copy this file to backend/.env and fill it in. .env is gitignored.
# With NOTHING set the app still runs: JSON-file database, emoji instead of
# photos, and any 4-digit OTP. Add credentials to switch each piece on.

# ── Firebase / Firestore ────────────────────────────────────────────
# From the Firebase console:
#   Project settings → Service accounts → Generate new private key
# Option A - paste the three fields (mind the quotes around the key):
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
# Option B - paste the whole downloaded JSON on one line, or base64 of it,
# and leave the three above blank:
# FIREBASE_SERVICE_ACCOUNT=
# Only if you use a named (non-default) Firestore database:
# FIRESTORE_DATABASE_ID=

# ── Cloudinary ──────────────────────────────────────────────────────
# Dashboard → Product Environment Credentials → "API environment variable"
CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@<cloud_name>
# All uploads are scoped under this folder:
CLOUDINARY_FOLDER=shanta-mahila-bazar

# ── MSG91 (login OTP) ───────────────────────────────────────────────
# Leave both blank in development and the code is shown on the OTP screen
# instead of being sent by SMS. It is a REAL six-digit code that is really
# checked, expires in five minutes and dies after five wrong guesses - not a
# bypass. Set BOTH of these to send by SMS instead.
#
# Production REFUSES TO BOOT without them, because demo mode returns the code
# in the HTTP response and anyone could then sign in as anyone.
#
# TWO WAYS TO SET THIS UP. Pick one.
#
# 1. WIDGET - no DLT registration needed, so this is the one to use.
#    MSG91 dashboard -> OTP -> Widget. The widget id is public (the browser
#    needs it, as VITE_MSG91_WIDGET_ID); the auth key is NOT and stays here.
#    The browser collects the code and gets a JWT; this server exchanges that
#    JWT for the verified number via /widget/verifyAccessToken.
MSG91_AUTH_KEY=
MSG91_WIDGET_ID=

# 2. PLAIN SENDER - needs your own DLT-approved template. We generate the code
#    and own its expiry, attempt cap and single use. Leave blank if using the
#    widget above; the widget wins when both are set.
MSG91_TEMPLATE_ID=
MSG91_SENDER=SMBAZR

# ── The first administrator ─────────────────────────────────────────
# Administrators are records in the database with scrypt-hashed passwords -
# create them with `npm run admin:users create <email> "<name>"`.
#
# These two exist only for a host with no shell, where that command cannot be
# run. While there is NO administrator at all, this one identity may sign in;
# the first successful login writes a real record and these are never consulted
# again. Delete them afterwards.
#
# The second is a HASH, never a password. Generate it with:
#   npm run admin:users hash
ADMIN_BOOTSTRAP_EMAIL=
ADMIN_BOOTSTRAP_NAME=
ADMIN_BOOTSTRAP_PASSWORD_HASH=

PORT=4000

# ── Danger: POST /api/dev/reset wipes the database ──────────────────
# Needs this AND a non-production NODE_ENV. Gating on NODE_ENV alone fails
# open, because it is unset by default on most hosts.
ALLOW_DEV_RESET=

# ── Danger: lets one write empty a collection ───────────────────────
# LEAVE THIS BLANK. No single persist may delete more than half a collection,
# because on 10 September 2026 one of them deleted every seller and every
# product - six real women - and only an hour of Firestore version history got
# them back.
#
# Set it for the one command that means it, never in a .env file:
#   ALLOW_BULK_DELETE=true npm run purge:demo -- --commit
ALLOW_BULK_DELETE=

# ── Session signing ────────────────────────────────────────────
# The key behind EVERY signature in the auth stack: session tokens, the
# phone-verification ticket registration requires, and the hashes that keep
# phone numbers out of the audit log.
#
# Required in production; development falls back to a fixed dev key.
# CHANGING IT SIGNS EVERYONE OUT - which is also how you revoke every session
# on the platform at once, if you ever need to.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# ── Demo data ──────────────────────────────────────────────
# When the database is EMPTY, load the demo sellers and products from
# seed.ts. Off by default: on a live database this would put invented
# sellers in front of real customers. Turn it on for a fresh local clone.
SEED_DEMO_DATA=

# ── CORS ───────────────────────────────────────────────────
# Which browser origins may call this API. COMMA-SEPARATED, because the
# seller/customer app and the admin site are deployed separately and both
# call this one backend. No trailing slashes (they are stripped anyway).
# Leave blank in development - blank means any origin.
# CORS_ORIGIN=https://shanta-bazar.vercel.app,https://shanta-admin.vercel.app
CORS_ORIGIN=

# ---------------------------------------------------------------------------
# WHERE THE ₹50 GOES
# ---------------------------------------------------------------------------
# The programme's own UPI account - what a seller's subscription screen shows
# her as a QR and as an ID she copies. Defaults to the college account in
# config.ts; set these only to move the money somewhere else.
#
# There is no account number or IFSC: she pays by UPI, and a wrong A/C number
# printed under a QR is worse than none. The NAME should be exactly what her
# UPI app displays after scanning, so she can check one against the other.
# ADMIN_UPI_ID=jasccollegeandur@sbi
# ADMIN_UPI_NAME=PRIN.JAWAHAR ARTS.SC.COM.
# ADMIN_BANK_NAME=State Bank of India
